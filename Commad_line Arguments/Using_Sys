import sys

#Returns the number of arguments plus the file name
print(f'Number of arguments: {len(sys.argv)}')

#Returns the file name and all arguments entered.
print(f'Argument List: {str(sys.argv)}')

arg_list = sys.argv[1:]
print(arg_list)